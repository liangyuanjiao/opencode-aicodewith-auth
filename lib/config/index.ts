export { deepEqual, stripJsonComments } from "./utils"
export { 
  applyProviderConfig, 
  buildStandardProviderConfig, 
  ensurePluginEntry, 
  isPackageEntry,
  type ApplyConfigResult,
} from "./sync"
